#This creates a Service account on the kubernetes cluster for prometheus
{{- if .Values.prometheus.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.appspec.name }}
  namespace: {{ .Values.global.namespace }}
